# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Publish Docker image

on:
  push:
    # CHANGED: Trigger only on changes to the Dockerfile
    paths: [ "Dockerfile" ]
  workflow_dispatch:
  
jobs:
  push_to_registry:
    name: Push Docker image to Docker Hub
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: write # CHANGED: Needed to push the new tag to the repo
      attestations: write
      id-token: write
    steps:
      - name: Check out the repo
        uses: actions/checkout@v3 # CHANGED: v5 does not exist yet, switched to v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # CHANGED: Added step to generate a date-based version string
      - name: Generate Version
        id: version
        run: echo "TAG=$(date +'%Y.%m.%d.%H%M')" >> $GITHUB_OUTPUT

      - name: Build and push Docker image
        id: push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          # CHANGED: Replaced metadata-action output with manual tags (Latest + Version)
          # REPLACE 'my-repo-name' below with your actual repository name!
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/dsci310-ia3-isaaca380-docker:latest
            ${{ secrets.DOCKER_USERNAME }}/dsci310-ia3-isaaca380-docker:${{ steps.version.outputs.TAG }}

      # CHANGED: Added step to push the same version tag to GitHub
      - name: Push Tag to GitHub
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.git.createRef({
              owner: context.repo.owner,
              repo: context.repo.repo,
              ref: 'refs/tags/v${{ steps.version.outputs.TAG }}',
              sha: context.sha
            })

      - name: Generate artifact attestation
        uses: actions/attest-build-provenance@v2
        with:
          # REPLACE 'my-repo-name' below
          subject-name: index.docker.io/${{ secrets.DOCKER_USERNAME }}/dsci310-ia3-isaaca380-docker
          subject-digest: ${{ steps.push.outputs.digest }}
          push-to-registry: true
